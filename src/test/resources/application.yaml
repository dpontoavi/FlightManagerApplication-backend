ktor:
    deployment:
        port: 8080
postgres:
    url: "jdbc:postgresql://localhost:5432/flightstudy"
    user: "postgres"
    password: "postgres"
    domain: "https://jwt-provider-domain/"
    audience: "jwt-audience"
    realm: "ktor sample app"
jwt:
    secret: ${JWT_SECRET}
    audience: ${JWT_AUDIENCE}
    domain: ${JWT_DOMAIN}
    realm: "FlightStudy"